# Backend de la app de clientes. Sobrescribe el DEFAULT_API_URL compartido
# (packages/shared/src/config/env.js) solo para esta app.
# Para un backend local, usa un .env.local (no se sube al repo).
EXPO_PUBLIC_API_URL=https://syscor-b5w6.onrender.com/api
